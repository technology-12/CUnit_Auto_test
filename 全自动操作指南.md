# CUnit 全自动化单元测试 —— 操作指南

本指南说明如何对任意 C 工程**一键全自动**生成并运行 CUnit 单元测试:按函数拆分上传大模型(避免 token 超限)、对数值计算函数生成边界值与预期值测试、自动编译运行(无需手写编译指令),并产出 MC/DC 报告。

> 配套文件:`cunit_mcdc_tool.py`(命令行核心)、`cunit_mcdc_gui.py`(图形界面)、`APIKEY.txt`(大模型连接配置)。

---

## 1. 前置条件

| 组件 | 说明 | 本机已验证路径 |
|------|------|----------------|
| Python | 3.8+(`from __future__` 注解需要 3.8 以上,本机为 3.14) | — |
| C 编译器 | GCC(MSYS2 ucrt64 或 mingw64) | `C:\msys64\ucrt64\bin\gcc.exe` |
| CUnit | C 单元测试框架 | 头文件 `C:\msys64\ucrt64\include\CUnit\Basic.h`,库 `C:\msys64\ucrt64\lib\libcunit.a` |
| 大模型 API | OpenAI 兼容的 chat/completions 接口 | 智谱 GLM(`APIKEY.txt` 里的地址) |

> 编译器与 CUnit 路径**留空可自动探测**。工具会按 `CUNIT_HOME`/`CUNIT_ROOT` 环境变量、`C:/msys64/{ucrt64,mingw64}`、`D:/msys64/...` 顺序查找 `<root>/include/CUnit/Basic.h` 与 `<root>/lib/libcunit*`,命中即用。

确保 MSYS2 的 `bin` 目录在 `PATH` 中(否则 `gcc` 找不到):
```bat
set PATH=C:\msys64\ucrt64\bin;%PATH%
```

---

## 2. 配置大模型 —— `APIKEY.txt`

`APIKEY.txt` 是大模型连接的**唯一配置来源**(URL / 模型 / Key 三件套),格式如下,顺序不限:

```
URL: https://open.bigmodel.cn/api/paas/v4
MODEL: glm-5.1
API-KEY: 你的Key
```

- `URL`:OpenAI 兼容接口基址(工具会自动在末尾拼接 `/chat/completions`)。
- `MODEL`:模型名。
- `API-KEY`:密钥,**只写入进程环境变量,不会落盘到 JSON**。

工具按以下顺序自动发现 `APIKEY.txt`:配置文件所在目录 → `project_root` → 当前工作目录。也可用 `--apikey-file` 显式指定。

---

## 3. 工程配置 —— `cunit_mcdc_config.json`

用示例初始化一份,再改成你的工程:
```bat
python cunit_mcdc_tool.py init -o work\config_new.json
```

关键字段(以 `work/` 示例工程为参考):

```json
{
  "project_root": "C:/.../outputs/work",
  "source_globs": ["src/**/*.c", "include/**/*.h"],
  "include_dirs": ["include", "src"],
  "test_output": "tests/auto_mcdc_tests.c",
  "build_commands": [],
  "test_commands": [],
  "coverage_commands": [],
  "llm_base_url": "https://open.bigmodel.cn/api/paas/v4",
  "llm_api_key_env": "OPENAI_API_KEY",
  "llm_model": "glm-5.1",
  "auto_build": true,
  "compiler": "gcc",
  "cunit_include_dir": "",
  "cunit_lib_dir": ""
}
```

- 全自动模式下 `build_commands`/`test_commands`/`coverage_commands` **留空**即可,由 `auto_build` 接管编译与运行。
- `cunit_include_dir`/`cunit_lib_dir` 留空走自动探测。
- `llm_base_url`/`llm_model` 若与 `APIKEY.txt` 冲突,**以 `APIKEY.txt` 为准**。

---

## 4. 命令行全自动(三步)

```bat
:: 第 1 步:确认能扫到判定与函数(可选,便于检查)
python cunit_mcdc_tool.py scan -c work\config_new.json

:: 第 2 步:按函数逐个生成 CUnit 测试(避免 token 超限;含数值边界值+预期值)
python cunit_mcdc_tool.py generate -c work\config_new.json --per-function

:: 第 3 步:生成 + 自动编译 + 运行 + 出报告(一步到位)
python cunit_mcdc_tool.py run -c work\config_new.json --generate --per-function --auto-build
```

第 3 步会:
1. 按函数调用大模型,合并成单个 `test_output` 测试文件;
2. 自动探测 CUnit 路径,自动编译(自动跳过工程里定义了 `main()` 的源文件,避免重复符号);
3. 运行测试可执行文件;
4. 解析 CUnit 输出,生成 `*.mcdc_report.json` 与 `*.mcdc_report.html`。

> 不想重新生成、只重跑编译运行(省钱省时):去掉 `--generate`。
> 想看会发给大模型的提示词但不实际调用:`generate -c ... --per-function --dry-run-prompt`。

### 实测结果(work 示例工程)
```
Auto-detected CUnit include: C:\msys64\ucrt64\include
Auto-detected CUnit lib:     C:\msys64\ucrt64\lib
Skipping (defines main): src\main.c
Compiling: gcc -Iinclude -Isrc -I...\include ... -lcunit -lm -o auto_mcdc_tests.exe
Run Summary:   tests   67   67   67    0
             asserts  173  173  173    0
```
67 个用例、173 个断言全部通过。

---

## 5. 图形界面全自动

```bat
python cunit_mcdc_gui.py
```

1. 顶部 **打开** 你的 `config_new.json`(或 **新建示例** 后编辑)。
2. 顶部 **加载 APIKEY.txt**(或确认 `APIKEY.txt` 在工程目录/当前目录,会自动发现)。
3. 「命令」标签页底部勾选 **启用自动编译运行**;编译器与 CUnit 目录可留空。
4. 点击 **生成并运行**。

界面右下「日志」看编译/运行输出,「报告」看测试用例结果与 MC/DC 义务。

---

## 6. 报告解读

每个工程会在 `test_output` 同目录生成两个报告:

- `auto_mcdc_tests.mcdc_report.json` —— 程序读取。含:
  - `test_case_results`:每个用例的名称 / 状态(passed/failed)/ 推断的被测函数。
  - `cunit_summary`:用例与断言总数/通过/失败(例如 `total:67, passed:67, asserts_total:173`)。
  - `decisions` + 每个判定的 `mcdc_obligations`:需确认的独立影响条件。
  - `coverage_summary`:若配置了覆盖率命令,含行/分支覆盖率与未覆盖行样例。
- `auto_mcdc_tests.mcdc_report.html` —— 人工查看。卡片展示判定数、覆盖率、流程状态、用例结果表、断言统计。

> **重要**:行覆盖/分支覆盖不能单独证明 MC/DC。报告把每个判定的"独立影响条件义务"固化下来,供你审查生成的测试是否真的形成了"只改变一个条件、判定结果随之翻转"的测试对。

---

## 7. 常见问题

**Q: 报错 `multiple definition of 'main'` / `'clamp'`。**
A: 这是历史问题。工程自带 `main.c`(含 `main()` 与辅助函数)与测试文件的 `main()` 冲突。当前版本自动跳过定义了 `main()` 的源文件,已规避。若仍出现,确认 `main.c` 没有被测试文件依赖的、且无替代定义的函数。

**Q: 报错 `CUnit/Basic.h: No such file or directory` 或链接找不到 `-lcunit`。**
A: 在「命令」页填写 `CUnit Include 目录`(`C:\msys64\ucrt64\include`)与 `CUnit Lib 目录`(`C:\msys64\ucrt64\lib`),或在 JSON 设 `cunit_include_dir`/`cunit_lib_dir`。留空时若自动探测失败,通常是 CUnit 未安装或不在标准 MSYS2 路径。

**Q: 大模型报 token 超限 / 上下文过长。**
A: 使用 `--per-function`(按函数拆分上传),GUI 默认已勾选「按函数生成」。每个函数独立一次请求,工程再大也不会超限。

**Q: 数值计算函数没有边界值测试。**
A: 工具已对识别为"计算型"的函数(含 `+ - * /`、`sqrt/sin/pow`、复合赋值等)注入专门 prompt,要求覆盖最大值/最小值/正常值/异常值并断言精确预期值。可对 `add`/`factorial` 等函数查看生成结果。

**Q: 想用其它大模型(DeepSeek / OpenAI / 本地)。**
A: 改 `APIKEY.txt` 三行即可,或改 JSON 的 `llm_base_url`/`llm_model`。只要是 OpenAI 兼容的 `/chat/completions` 接口都行。

**Q: `API-KEY` 不想写进文件。**
A: 删掉 `APIKEY.txt` 的 `API-KEY:` 行,改用环境变量:`set OPENAI_API_KEY=你的Key`(变量名对应 JSON 的 `llm_api_key_env`)。GUI 里也可填「临时 API Key」,只进当前进程。
