# CUnit 全自动化单元测试工具 —— 用户手册

本工具对任意 C 工程**全自动化**生成并运行 CUnit 单元测试:扫描判定与函数 → 按函数拆分上传大模型(避免 token 超限)→ 对数值计算函数生成边界值与预期值测试 → 自动编译运行(无需手写编译指令)→ 产出 MC/DC 报告。

- 命令行核心:`cunit_mcdc_tool.py`
- 图形界面:`cunit_mcdc_gui.py`
- 大模型连接配置:`APIKEY.txt`(密钥,已被 git 忽略)
- 工程配置:`config_new.json`(或任意你命名的 JSON)

> 快速上手见 `全自动操作指南.md`;OSS-Fuzz-Gen 的互补作用见 `OSS_Fuzz_Gen_Integration.md`。本手册是字段与配置的完整参考。

---

## 一、两个配置文件各管什么

| 文件 | 管什么 | 是否提交 git |
|------|--------|--------------|
| `APIKEY.txt` | **大模型连接**:接口地址、模型名、密钥 | ❌ 不提交(密钥,已被 `.gitignore` 忽略);模板见 `APIKEY.example.txt` |
| `config_new.json` | **被测工程与构建**:工程路径、源码匹配、include 目录、编译器、是否自动构建等 | ✅ 提交(无密钥) |

两者职责分离:`APIKEY.txt` 只放密钥与模型信息,`config_new.json` 只放工程信息。**当 `APIKEY.txt` 存在时,其中的 `URL` 与 `MODEL` 会覆盖 `config_new.json` 里的 `llm_base_url`/`llm_model`**;密钥只写入进程环境变量,绝不落盘到 JSON。

---

## 二、`config_new.json` 字段详解

完整示例(基于 `work/config_new.json`):

```json
{
  "project_root": "./work",
  "source_globs": ["src/**/*.c", "include/**/*.h"],
  "include_dirs": ["include", "src"],
  "test_output": "tests/auto_mcdc_tests.c",
  "build_commands": [],
  "test_commands": [],
  "coverage_commands": [],
  "llm_base_url": "https://open.bigmodel.cn/api/paas/v4",
  "llm_api_key_env": "OPENAI_API_KEY",
  "llm_model": "glm-5.1",
  "max_llm_rounds": 1,
  "extra_prompt": "Prefer testing public APIs. ...",
  "auto_build": true,
  "compiler": "gcc",
  "cunit_include_dir": "",
  "cunit_lib_dir": ""
}
```

### 工程与扫描

| 字段 | 类型 | 默认 | 说明 |
|------|------|------|------|
| `project_root` | 字符串(路径) | `"."` | 被测 C 工程根目录。相对路径按**运行工具时的当前工作目录**解析(从 `outputs/` 运行时 `./work` 即指 `outputs/work`)。 |
| `source_globs` | 字符串数组 | `["**/*.c","**/*.h"]` | 要扫描的源文件 glob,**相对 `project_root`**。建议把 `.c` 和 `.h` 都纳入。注意:只匹配 `.c` 会漏掉同目录的头文件(工具会另行用 `include_dirs` 补齐 `#include`)。 |
| `include_dirs` | 字符串数组 | `[]` | 编译时的 `-I` 目录,相对 `project_root`。**工具还会从这里发现头文件并自动 `#include`**(按裸文件名),所以务必把存放头文件的目录都列上(例如 `include`、`src`)。 |
| `test_output` | 字符串(路径) | `tests/auto_mcdc_tests.c` | 生成的 CUnit 测试文件路径,相对 `project_root`。同名 `.exe` 与 `.mcdc_report.{json,html}` 会生成在它旁边。 |

### 命令模式(手动模式用;全自动模式留空)

| 字段 | 类型 | 默认 | 说明 |
|------|------|------|------|
| `build_commands` | 字符串数组 | `[]` | 手动构建命令,每条一行,在 `project_root` 下执行。**自动模式(`auto_build:true`)下忽略**,留空即可。 |
| `test_commands` | 字符串数组 | `[]` | 手动测试运行命令。自动模式下忽略。 |
| `coverage_commands` | 字符串数组 | `[]` | 手动覆盖率命令(如 `gcov`)。自动模式下忽略(自动模式不采集覆盖率,只跑测试)。 |

> 全自动模式下三项全部留空,由 `auto_build` 接管编译与运行。

### 大模型(可被 APIKEY.txt 覆盖)

| 字段 | 类型 | 默认 | 说明 |
|------|------|------|------|
| `llm_base_url` | 字符串 | `https://api.openai.com/v1` | OpenAI 兼容接口的**基址**(工具会自动拼接 `/chat/completions`;若你填的是完整端点,也能识别不重复拼接)。**`APIKEY.txt` 的 `URL` 会覆盖此项。** |
| `llm_api_key_env` | 字符串 | `"OPENAI_API_KEY"` | 存放密钥的**环境变量名**。工具把 `APIKEY.txt` 的密钥写入这个环境变量。多个工程可用不同变量名隔离。 |
| `llm_model` | 字符串 | `gpt-4.1` | 模型名。**`APIKEY.txt` 的 `MODEL` 会覆盖此项。** |
| `max_llm_rounds` | 整数 | `1` | 预留的迭代轮数(当前实际按 1 轮生成)。 |
| `extra_prompt` | 字符串 | `""` | 追加到**每一次** LLM 提示词末尾的额外约束,例如:只测 public API、stub 策略、include 规则、编码规范等。 |

### 自动编译运行

| 字段 | 类型 | 默认 | 说明 |
|------|------|------|------|
| `auto_build` | 布尔 | `false` | `true` 时:生成后自动编译测试文件并运行,忽略 `build/test/coverage_commands`。**全自动模式的核心开关。** |
| `compiler` | 字符串 | `"gcc"` | C 编译器可执行名(如 `gcc`/`clang`/`cl`)。 |
| `cunit_include_dir` | 字符串 | `""` | CUnit 头文件目录。**留空 → 自动探测**(按 `CUNIT_HOME`/`CUNIT_ROOT` 环境变量、`C:/msys64/{ucrt64,mingw64}` 等常见路径查找 `include/CUnit/Basic.h`)。 |
| `cunit_lib_dir` | 字符串 | `""` | CUnit 库目录。**留空 → 自动探测**(查找 `lib/libcunit*`)。 |

### 自动模式的行为细节(重要)

- 自动编译会**跳过定义了 `main()` 的源文件**(例如 `src/main.c`),避免与测试文件自带的 `main()` 冲突(`multiple definition of 'main'`)。
- 抽取器**不会**给入口文件(定义 `main` 的 `.c`)和 `static` 函数生成测试——它们无法在独立翻译单元中被调用。
- 生成的测试文件用**裸头文件名**(`#include "math_ops.h"`)并通过 `-I` 解析,不受测试文件所在目录或 Windows 路径分隔符影响。

---

## 三、`APIKEY.txt` —— 大模型连接配置(唯一)

本文件集中配置大模型接口,**密钥只存在于此文件 + 进程环境变量**,不进 `config_new.json`、不进 git。

### 格式 A:前缀格式(推荐)

```
URL: https://open.bigmodel.cn/api/paas/v4
MODEL: glm-5.1
API-KEY: 你的密钥
```

支持的字段名(大小写、空格、`-`/`_` 不敏感):`URL`/`BASE_URL`/`API_BASE`/`ENDPOINT`、`MODEL`、`API-KEY`/`API_KEY`/`KEY`。

### 格式 B:裸格式(一行一项)

```
sk-xxxxxxxxxxxx                       # 密钥(以 sk- 开头,或智谱的 32位hex.后缀)
https://api.xiaomimimo.com/v1         # 接口地址
mimo-v2.5-pro                         # 模型名(可选)
```

> 解析器对两种格式都兼容,可混用。找不到 `MODEL` 时沿用 `config_new.json` 的 `llm_model`。

### 自动发现顺序

工具按以下顺序查找 `APIKEY.txt`:① `--apikey-file` 显式指定的路径 → ② 配置文件所在目录 → ③ `project_root` → ④ 当前工作目录。命中即用。

### 切换供应商示例

智谱 GLM:
```
URL: https://open.bigmodel.cn/api/paas/v4
MODEL: glm-5.1
API-KEY: <智谱 key>
```

DeepSeek:
```
URL: https://api.deepseek.com/v1
MODEL: deepseek-chat
API-KEY: <deepseek key>
```

小米 MiMo(裸格式):
```
sk-<mimo key>
https://api.xiaomimimo.com/v1
mimo-v2.5-pro
```

OpenAI:
```
URL: https://api.openai.com/v1
MODEL: gpt-4.1
API-KEY: <openai key>
```

> 任何兼容 OpenAI `/chat/completions` 协议的接口都可用。

---

## 四、命令行用法(CLI)

```bat
:: 初始化示例配置
python cunit_mcdc_tool.py init -o my_config.json

:: 扫描判定(不调 LLM,用于检查识别结果)
python cunit_mcdc_tool.py scan -c work\config_new.json

:: 预览提示词但不调用
python cunit_mcdc_tool.py generate -c work\config_new.json --per-function --dry-run-prompt

:: 按函数生成测试(自动发现 APIKEY.txt)
python cunit_mcdc_tool.py generate -c work\config_new.json --per-function

:: 一键全自动:生成 + 自动编译 + 运行 + 出报告
python cunit_mcdc_tool.py run -c work\config_new.json --generate --per-function --auto-build

:: 指定 APIKEY 文件 / 不重新生成只重跑
python cunit_mcdc_tool.py run -c work\config_new.json --apikey-file APIKEY.txt
```

常用参数:

| 参数 | 说明 |
|------|------|
| `-c / --config` | JSON 配置文件路径(必填) |
| `--per-function` | 按函数逐个生成(避免 token 超限,推荐) |
| `--auto-build` | 自动编译运行(覆盖配置里的 `auto_build`) |
| `--generate` | `run` 子命令下:先重新生成测试再编译运行 |
| `--apikey-file` | 显式指定 APIKEY 文件,默认自动发现 |
| `--dry-run-prompt` | 只打印提示词,不调用大模型 |

---

## 五、图形界面(GUI)

```bat
python cunit_mcdc_gui.py
```

- 顶部:**打开/保存/另存**配置、**新建示例**、**加载 APIKEY.txt**。
- 「基础配置」:工程目录、源码匹配、Include 目录、测试输出、模型等(对应 `config_new.json`)。
- 「命令」:手写构建/测试/覆盖率命令;底部「自动编译运行」面板:勾选启用、编译器、CUnit Include/Lib 目录(留空自动探测)。
- 「提示词补充」:`extra_prompt`。
- 右侧:MC/DC 判定表、日志、报告。

GUI 也会自动应用 `APIKEY.txt`(无需手动点加载,只要文件在工程目录或当前目录)。

---

## 六、产物与报告

一次 `run --generate --auto-build` 会在 `test_output` 同目录生成:

| 文件 | 内容 |
|------|------|
| `auto_mcdc_tests.c` | LLM 生成的 CUnit 测试源码(按函数合并,自带 `main` 与注册) |
| `auto_mcdc_tests.exe` | 编译产物(自动模式) |
| `auto_mcdc_tests.mcdc_report.json` | 程序读取:用例结果、断言汇总、判定与 MC/DC 义务、覆盖率 |
| `auto_mcdc_tests.mcdc_report.html` | 人工查看:流程状态、覆盖率卡片、用例结果表、MC/DC 义务 |

报告关键字段:

- `cunit_summary`:用例/断言的 `total / ran / passed / failed`(来自 CUnit Run Summary)。
- `test_case_results`:每个用例的 `name / status(passed|failed) / function(推断的被测函数)`。
- `decisions[].mcdc_obligations`:每个判定需确认的「独立影响条件」对。

> **重要**:行覆盖/分支覆盖不能单独证明 MC/DC。报告把每个判定的独立影响义务固化下来,供审查生成测试是否形成了「只改变一个条件、判定结果随之翻转」的测试对。

---

## 七、常见问题

| 现象 | 原因 / 处理 |
|------|-------------|
| 生成报 `HTTP 429` / 智谱 `1113` | 账户余额不足,充值或换 key,**非代码问题** |
| 生成报 `HTTP 402` | (如 MiMo)账户余额不足,同上 |
| `HTTP 403` / 智谱 `1220` | 该 key 无该模型权限,换 `APIKEY.txt` 里有权限的 `MODEL` |
| `CUnit/Basic.h: No such file` | 填 `cunit_include_dir`,或留空让其自动探测;确认 CUnit 已安装 |
| 链接找不到 `-lcunit` | 填 `cunit_lib_dir`,或留空自动探测 |
| `multiple definition of 'main'` | 已自动规避(自动编译跳过定义 main 的源文件);若仍出现,检查是否用了手动命令把 `main.o` 编进去 |
| 数值函数没有边界值测试 | 工具已对计算型函数注入边界值 prompt(最大/最小/正常/异常 + 精确预期值);确认该函数被 `_is_computation_function` 识别 |
| token 超限 / 上下文过长 | 使用 `--per-function`,GUI 默认已勾选 |

---

## 八、最小可运行示例(`work` 工程)

```bat
cd outputs
:: 1. 配好 APIKEY.txt(从 APIKEY.example.txt 复制并填密钥)
:: 2. 一键全自动
python cunit_mcdc_tool.py run -c work\config_new.json --generate --per-function --auto-build
```

参考结果(glm-5.1):10 个被测函数 → 73 个用例、152 个断言,全部通过。
